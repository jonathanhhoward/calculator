import { render } from "@testing-library/angular";
import { EqualsKeyComponent } from "./equals-key.component";

describe("EqualsKeyComponent", () => {
  it("should render", async () => {
    await render(EqualsKeyComponent);
  });
});
